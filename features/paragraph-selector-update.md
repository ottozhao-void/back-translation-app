

# Overview

顶部的段落导航组件需要根据译文的状态来显示不同的颜色：
1. 当译文是 draft 类型时，视为未完成，高对比度边框和文字，透明背景色。
2. 当译文是 llm 类型时，当得分大于预设阈值时（需要在设定页面增加设置阈值的部分，默认为85），显示绿色背景，小于时，显示黄色背景
3. 至于类型 diff, 显示黄色背景

另外，当用户选定文章和翻译模式后，进入到Practice Session时，自动跳转到第一个未完成段落